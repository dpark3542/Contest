#include <bits/stdc++.h>

using namespace std;

void solve(istream& in, ostream& out) {

}

int main() {
  signal(SIGABRT, [](int) { throw exception(); });
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);

  int t;
  cin >> t;
  while (t--) {
    solve(cin, cout);
  }

  return 0;
}
